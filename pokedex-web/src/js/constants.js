const STATES = {
    ACTIVE: 'active',
};

const PAGE_SIZE = 10;

// This should move out once the items here are more useful
const SETTINGS_ITEMS = [
    {
        key: "Version",
        value: "1.0",
    }
]

module.exports = {
    PAGE_SIZE,
    STATES,
    SETTINGS_ITEMS,
}